# API Configuration
# Core API endpoint for service communication
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Base application URL for client-side routing
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Current environment (development, staging, production)
NEXT_PUBLIC_ENVIRONMENT=development

# Payment Processing
# Stripe publishable key for client-side payment integration (v2023-10)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Maps Integration
# Mapbox access token for property location visualization (v2.0)
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_access_token

# Analytics and Monitoring Configuration
# Segment write key for event tracking (v2.0)
NEXT_PUBLIC_SEGMENT_WRITE_KEY=your_segment_write_key

# Sentry DSN for error monitoring (v7.x)
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Mixpanel token for user analytics (v2.x)
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token

# IMPORTANT SECURITY NOTES:
# - Never commit actual values to version control
# - Use different values for development, staging, and production
# - Ensure proper key rotation and secure credential management
# - Only NEXT_PUBLIC_ prefixed variables are exposed to client-side
# - Validate all required variables during build process